import { Terrain } from './terrain';

export class Player {
    constructor(x: number, y: number, health: number, attack: number, context: CanvasRenderingContext2D) {
        // Initialize player
    }

    move(dx: number, dy: number) {
        // Move player
    }

    dig(terrain: Terrain) {
        // Implement digging
    }

    draw() {
        // Draw player on the context
    }
}